import { PdidiscountPipe } from './pdidiscount.pipe';

describe('PdidiscountPipe', () => {
  it('create an instance', () => {
    const pipe = new PdidiscountPipe();
    expect(pipe).toBeTruthy();
  });
});
